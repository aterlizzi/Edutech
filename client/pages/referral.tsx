import React from "react";
import Layout from "../components/layout";

function Referral() {
  return <div></div>;
}

Referral.getLayout = (page) => (
  <Layout title="Referrals - Edutech">{page}</Layout>
);

export default Referral;
